# Tick journal

The loop's memory. The conversation is not: compaction erases it, and the next tick is usually a
fresh session. One line per tick, appended, never rewritten.

```text
2026-08-06T02:26Z · deliver · preflight · — · Blocked · no RUNBOOK.md · next: /deliver:install-runbook
2026-08-06T02:30Z · install-runbook · — · — · Success · RUNBOOK.md + fixture + 3 evals + .http + run skill · L1 13/13 integration
2026-08-06T02:31Z · deliver · rule 4 · #10 · Blocked-cleared · preflight green, #10 selected · handoff deferred to next firing
2026-08-06T02:56Z · deliver · rule 4 · #10 · Success · PR #23 opened, card In Review · L1 build+15+20, L3 AG-UI+audit+seed-fix-on-persisted-volume
2026-08-06T03:05Z · deliver · rule 4 · — · Blocked · all 3 Ready items (#11 #12 #13) compile against epic 2 entities absent from main; #10 open pending PR #23 · next: merge #23
2026-08-06T03:26Z · deliver · rule 4 · — · Blocked (2nd identical) · nothing changed since 03:05; #23 still open, epic-2 entities still absent from main · a 3rd is Stalled — stop the timer or merge #23
2026-08-06T12:32Z · deliver · preflight · — · Blocked · Docker daemon not running (npipe absent) — rung 3 and the runtime proof both need it · next: start Docker Desktop
2026-08-06T12:32Z · note · — · #10 · — · PR #23 merged, #10 closed, epic-2 entities on main; board card was stale at In Review and was reconciled to Done
2026-08-06T12:57Z · deliver · rule 4 · #11 · Success · PR #24 opened, card In Review · L1 build+27+28, L3 AG-UI+audit+catalog; grounding seen red before green
2026-08-06T13:06Z · deliver · rule 4 -> 5 · — · No-op · review is the constraint, not build capacity: #12 and #13 both need Decision.evidenceRef -> ScreeningResult, which is in open PR #24 · next: review/merge #24
2026-08-06T16:32Z · deliver · rule 4 · #12 · Success · PR #29 opened, card In Review · L1 build+27+39(x2), L3 approval_required fired + call parked + sourcer/recruiter denied; gate guard seen red
2026-08-06T22:57Z · deliver · rule 4 · #13 · Success · PR #36 opened, card In Review · L1 build+28+50(x2), L3 approval_required fired; instruction guard seen RED against real code (#12 defect); filed #35 role-baseline drift
2026-08-06T23:06Z · deliver · rule 5 · — · No-op · zero Ready; epic 3 is complete and #36 is the only thing in flight. Reconciled 2 stale In Review cards (#11 #12, both merged) to Done · next: /plenipo:define to promote from Backlog (readyFloor 3)
```

2026-08-06T23:05Z · ship · 1 open · 0 merged · #36 REVIEWED (new, unlabelled) → escalate + needs-human: it ADDS `tools.hiring.reject_candidate` to the `hiring-recruiter` baseline in Program.cs, and pr-gates.mjs scans only REMOVED lines by design, so `spine_untouched` stays silent on a permission widening that //domain-note says must stay human at every level · code itself is sound — all 7 ACs met, instruction-guard test verified genuinely red on base, both edited assertions tighten, tenant filters untouched · also blocked independently by `checks_exist` (ZERO workflow runs on the branch) and `mergeable` (BEHIND) · recruiter already holds ManageApprovals, so post-merge the same human can reject and self-approve — matches SPEC.md §3/§6, flagged not blocked
2026-08-06T23:05Z · ship · NOTE · GitHub Actions has NOT recovered — the fleet's 21:05Z "RECOVERED" call was wrong, and the stall has only changed shape · that call rested on "0 queued and 0 in progress across all four products", but zero-and-zero is equally consistent with NO EVENT PRODUCING A RUN AT ALL, and every run it cited as evidence predated the observation. Evidence here: PR #36 opened 22:56Z by a human PAT, with ci.yml (`on: pull_request`, no path filter) and agent-gates.yml (`on: pull_request: [opened,…]`) both obliged to fire, produced ZERO runs. Nothing has run in this repo since 18:49Z and the agent-merge */15 cron has missed ~17 consecutive firings. Actions reads enabled:true, allowed:all. Consequence: `checks_exist` is unsatisfiable, so ship can review but can NEVER merge — and the unattended merger the level-3 posture assumes is live has not run in over four hours.
2026-08-06T23:28Z · deliver · preflight · — · Blocked · dirty tree: 3 uncommitted TICKS.md journal lines from the 23:05Z ship tick, committed here as journal preservation, not code · zero Ready so rule 5 would have fired anyway
2026-08-06T23:28Z · deliver · NOTE · #36 · — · CORRECTION to the 23:05Z ship NOTE: GitHub Actions HAS recovered — runs at 23:12/23:14/23:17/23:22 on both main and the branch, workflows all active, permissions enabled. That NOTE was true when written and is now stale.
2026-08-06T23:28Z · deliver · NOTE · #36 · — · Its required PR gates check was RED on has_red_before_green, and the gate was RIGHT: my Regression-test section said "seen red" but never said it went green, and section() requires BOTH /red/i and /green/i between the heading and the next one. Fixed the PR BODY, not the gate. Local re-run: 3/3 ok, exit 0.
2026-08-06T23:38Z · deliver · NOTE · #36 · — · Actions is NOT stalled: trigger latency is ~4.5 min. Runs appeared for head b4643f3 after 270s of polling. Both earlier calls (fleet 21:05Z RECOVERED, ship 23:05Z STILL-DEAD) and my own two were all sampling a system whose latency exceeds the observation window. Any tick that reads checks promptly after a push will see checks_exist unsatisfiable and be wrong.
2026-08-06T23:38Z · deliver · NOTE · #36 · — · Required checks now GREEN: PR gates pass (body fixed, not the gate), build pass. mergeState=UNSTABLE because the non-required `agent` check fails: the gh-aw approval-verdict workflow cannot authenticate to its Copilot provider (HTTP 401, transient_auth_error). So the automated approval verdict PR #31 installed is NOT operational — a human verdict is the only one available. Still labelled needs-human for the permission-widening escalation, which is independent of all of this.
2026-08-06T23:45Z · deliver · rule 5 · — · No-op · zero Ready, third deliver tick running with nothing buildable (23:06 No-op, 23:28 Blocked, now No-op). Not Stalled by the letter — Blocked is not in the detector list and every tick committed a journal — but it is the same wall three times, and only /plenipo:define moves it.
2026-08-06T23:54Z · define · ready 0→2 · promoted #19 #21 · no epic added (Ready+Backlog=8, well above floor) · triage empty: 0 needs-triage, 0 enhancements · L2
2026-08-06T23:54Z · define · Approval-required · floor of 3 NOT met and deliberately so. #14/#15 shaped (ARCH §5 + ADR-0011 pin @plenipo/ui 0.1.0-alpha.28, defineModule/PlenipoApp, AddViteApp+WithPnpm — all read from platform source) but BLOCKED on OQ9: @plenipo/ui is not on public npm and networthy has no .npmrc or vendored tarball, so a frontend may break keyless CI. #16/#17/#20 blocked by the human-recorded rule in workflow.json //what-3-means-here: a pr-gates.mjs PATH_RULE for consent and impact-report paths must exist first. Two named human decisions, not invented work.
2026-08-06T23:57Z · define · ready 2→4 · promoted #14 #15 · OQ9 RESOLVED at L1: @plenipo/ui is on PUBLIC npm and a probe install with a blank _authToken exited 0 with 0.1.0-alpha.28 on disk. Keyless CI holds. networthy having no .npmrc was evidence none is needed, not a gap. Asymmetry recorded: .NET packages vendored (not on nuget.org), npm package public (must not be vendored). Ready now meets readyFloor 3.
2026-08-07T00:15Z · deliver · rule 4 · #14 · Success · PR #39 opened, card In Review · first frontend: @plenipo/ui keyless, CvHighlighting server-side, CI now builds the UI · L1 build+36+44+vite, L3 tab dataEndpoint=null + segments rebuild 759-char CV + 403/404 · highlighting seen RED (naive per-citation impl fails overlap + out-of-order)
```
2026-08-07T00:35Z · deliver · rule 1 (red check, not the label) · #39 · Success · fixed the red `build` on an already-open PR rather than starting a 4th — ceiling was 3/3 so rule 4 could not fire anyway. CI caught a half-answered pnpm PROMPT committed as pnpm-workspace.yaml (`esbuild: set this to true or false`); root cause was two pnpm majors, 9 hardcoded in ci.yml vs 11.9.0 local + AppHost .WithPnpm(). Version now declared ONCE via packageManager. esbuild build-script answered as an allowlist of one. · L1 red-then-green locally on BOTH majors (npx pnpm@9 reproduced the exact error text), L3 CI green on a clean runner: Install/Build — frontend SUCCESS · preflight dirty tree was a PHANTOM: the *.tsbuildinfo ignore rule already exists, unmerged, inside #39 · PR gates still RED on #39 and #38 by design — spine_untouched wants a human on workflow changes and I did NOT self-apply human-approved
2026-08-07T00:47Z · deliver · rule 5 · — · No-op · review is the constraint, not build capacity. Open PRs 3/3 (#36 #38 #39), all with build=pass and reviews=0 — no human has looked since the last tick, so there was nothing an agent could legitimately advance. No agent:changes-requested, no In Progress card, no p0 bug (#35 is p1). NOT Stalled by the letter — both prior ticks committed — but this is the second consecutive tick that could not start work, and a timer pointed here will now emit No-op indefinitely until someone reviews. Two of the three need a human `human-approved` label for spine_untouched (workflow-file changes) which the agent must not self-apply; #36 needs a human for the permission widening pr-gates structurally cannot see.

2026-08-07T13:20Z · ship · 1→2 open · 0 merged · #38 REVIEWED (was unlabelled, 0 reviews) → agent:changes-requested, 4 blocking: (1) the label DELETE is `if gh api -X DELETE … >/dev/null 2>&1`, so 403/401/429/5xx are indistinguishable from "label not present", the run goes green and the stale `agent:approved` rides into the 15-min merge cron — the one control the PR adds is silent when it fails; (2) `types: [synchronize]` misses base RETARGET (`edited` + `changes.base`, no head-SHA change) and `reopened`, while this repo's own agent-gates.yml:13 lists both and says on lines 7-8 why — and pr-approval-verdict.md:3-4 omits `edited` too, so a retarget gets neither a reset nor a re-review; (3) the cited `/harness:validate-product` detector does not exist on plenipo-agents@main nor in the installed plugin tree, and as described it asserts file EXISTENCE, which an empty workflow would satisfy; (4) "fixed at source in plenipo-agents" is false — setup/assets/ has no agent-approval-reset.yml, WORKFLOWS.md never mentions it, no open PR and no branch, newest commit #23 at 08-06 20:01Z predates this PR by ~4h · #41 NOT reviewed: created 13:08Z, two minutes into this tick, checks_exist red because no run had started and mergeable=UNKNOWN — next tick's work · gates on #38 unchanged: checks_green (PR gates, agent), mergeable BEHIND, agent_approved
2026-08-07T13:20Z · ship · NOTE · the sibling reviews were treated as hypotheses and one of them is WRONG in a way that helps this PR · casewell 01:20Z reported "nothing shows this workflow has ever executed, so a YAML parse error would look identical". Refuted here at L3: the byte-identical file is registered `active` in networthy with 14 successful pull_request runs, and run 31012041461 (08-05 13:49Z) printed `cleared  agent:approved`. The YAML parses, the event name is right, `pull-requests: write` suffices. Evidence the author had and did not cite — the body would be stronger for it than for the 404 it does cite. It does NOT rescue findings 1-2, which run through exactly the branch that swallows failures. Also narrowed finding 1's fork route: forkCount is 0 and pr-approval-verdict.lock.yml:53-59 runs on plain `pull_request`, so a fork PR's own token could never have applied the label — the fork case needs a human labeller. Blocking regardless.
2026-08-07T13:20Z · ship · NOTE · `has_red_before_green` passed on #38 over a section containing no test, and this is the third product where that has now been observed · pr-gates.mjs:73 is `/red/i.test(regression) && /green/i.test(regression)` — a prose regex over the section text. #38's "red" is a `gh api` 404 for the file the PR adds, which ANY file at that path turns green, including an empty one. That is the mechanism by which finding 3 reached review. Same hole recorded in casewell 01:20Z and implicated in auditworthy 00:09Z. A defect of the gate, not of these PRs; it deserves an issue in all four products and a fix at the plenipo-agents source rather than four forks.

2026-08-07T16:35Z · ship · 2 open · 0 merged · Success (reviewed and correctly blocked) · #41 REVIEWED (was unlabelled, 0 reviews) → agent:changes-requested, 4 blocking. The PR is named after two changes and NEITHER is proven: (1) the stale-verdict fix at merge-gate.mjs:323-334 is unreachable from its own test — `SIMULATE` is true under `--fixture`, so refreshMainGreen(), the fresh `gh pr view`, the re-evaluate and the `continue` all sit inside `if (!SIMULATE)` while the counted `RECHECK #` log sits outside it; the reviewer DELETED lines 325-333 and the suite still printed `ok  --merge — every merge after the first re-reads the world` and exit 0, so the body's headline "Both seen red before green" is false and the body itself concedes the merge-gate red was the fixture-#901 crash, i.e. defect 3's red not defect 2's; (2) `reviewer_down`, the detector written from the 401 outage, is silenced by a sibling workflow — REVIEWER=/approval|intent.?review|verdict/i at loop-heartbeat.mjs:167 also matches `Reset agent approval` (active, arrives via #38, green on every push), and assess() requires all 3 newest matches to be failures, so one green reset run at the head clears it: at 13:15 today the verdict workflow had 10 consecutive failures and the heartbeat would have called the loop healthy; (3) readState() at :154-178 is the only code joining detectors to reality and has no test and no fixture — a wrong field name or status literal yields empty arrays forever, permanent silence indistinguishable from health; (4) merger_stalled/prs_rotting exclude neither drafts nor HOLD_LABELS nor agent:changes-requested, so a deliberately parked PR (this one, which must be human-merged) pins the heartbeat issue open forever, contradicting the file's own "biased toward silence". Nine non-blocking notes incl. N3: strict:true is already set on main, so GitHub would have 405'd the second merge anyway — the prior behaviour was a crashed run, not an unverified merge, and issue #40 overstates it for this repo · L1 on the deletion experiment, both suites, the runners_starved red, the assess masking demos; L4 on the re-check working on the real path · #38 unchanged: already agent:changes-requested from 13:20Z, and github-actions posted an independent verdict at 16:15Z confirming findings 1-2 — no second comment added · 0 READY, 0 merged, `--merge` never run
2026-08-07T16:35Z · ship · NOTE · the 401 is NOT the binding constraint this tick, so this is not Blocked · both PRs also fail spine_untouched (they edit .github/) and both now carry agent:changes-requested on substance. The standing `agent` check HTTP 401 (Copilot provider auth, run 31181776488 job 92876680125: `models fetch returned 401`) is real and still needs a human, but even a fixed token merges nothing here — #38 and #41 each need a human `human-approved` for the workflow-file changes AND a revision pass. Not self-applied, not routed around. Notably the 401 does not stop the gh-aw verdict workflow from posting: its `agent` job goes red while `safe_outputs` and `conclusion` still succeed, which is how #38 got a second review at 16:15Z.

2026-08-07T16:51Z · ship · 2 open · 0 merged · 0 reviewed · No-op · nothing to review and nothing to merge, 16 minutes after the previous tick. Both open PRs already carry a CURRENT adversarial verdict, so step 3 had no candidate: #38 relabelled agent:changes-requested at 16:15:18Z over a head commit of 16:10:06Z, #41 at 16:30:54Z over 13:15:05Z — in both cases the verdict post-dates the newest commit, so neither is stale under the very rule #38 exists to add. `--merge` never run. Not Blocked, not Approval-required by the letter: Approval-required wants a PR that passed EVERY automated gate, and neither did. No-op is the honest state even though the letter of it ("waiting on a check that has not finished") does not fit either — the checks finished and said no.
2026-08-07T16:51Z · ship · NOTE · spine_untouched reproduced locally at L1 rather than read off a red X, because the "PR gates" check-run exposes neither output.summary nor a --log-failed body — `gh run view --log-failed` returns empty for it, so the API gives you FAILURE and no reason. Re-ran the same script on the same diffs: #38 fails on 1 path (.github/workflows/agent-approval-reset.yml), #41 on 7 (loop-heartbeat.mjs + .test + fixtures/heartbeat.json, merge-gate.mjs + .test, agent-gates.yml, agent-heartbeat.yml). Both print "A human must review it. Add the `human-approved` label to override" — not applied, not applicable to me. closes_an_issue, has_runtime_evidence and has_red_before_green all pass on both; spine_untouched is the sole pr-gates failure on each. Worth recording that this repo's harness PRs will ALWAYS trip spine_untouched by construction: every improvement to the loop's own gates edits .github/, so the loop can author them and can never merge them. That is the design working, not a snag.
2026-08-07T16:51Z · ship · NOTE · the standing 401 is still not the binding constraint. The `agent` check is red on #41 (Copilot provider auth) and green on #38, and per the scheduled-task brief a Blocked report is only honest when that 401 is the ONLY thing between a PR and merge. It is not: each PR independently needs (a) a human `human-approved` for spine_untouched, (b) a revision pass answering an agent:changes-requested verdict, and (c) a rebase off BEHIND. Fixing the token today merges nothing here. Both PRs are `deliver` rule 1 work, not `ship` work.

2026-08-07T18:30Z · deliver · rule 4 · #15 · Success · PR #42 opened, card In Review, labelled needs-human · Pipeline board: list_applicants read tool + pipeline tab + GET /api/hiring/pipeline + custom-React board with drag · L1 build 0-warn + 37 module + 62 integration (incl. new eval case) + pnpm build; L3 booted host — RBAC matrix 200/200/200/403, 7 write shapes all 404/405, AG-UI approval_required fired on advance, compliance offered ZERO tools, board driven in a browser with two real drags and server truth unchanged · 4 tests seen RED→green; the no-write GUARD passed vacuously so I proved it by temporarily adding the forbidden MapPut and watching it go red, then deleting it
2026-08-07T18:30Z · deliver · NOTE · #15 · — · HELD FOR A HUMAN and NOT self-approved: the PR widens permission grants (adds tools.hiring.list_applicants to the sourcer and recruiter baselines), which workflow.json //domain-note says stays human at EVERY level. pr-gates.mjs spine_untouched scans REMOVED lines only, so a widening is structurally invisible to it — same gap that escalated #36. `human-approved` not applied.
2026-08-07T18:30Z · deliver · NOTE · #15 · — · THE DESIGN DECISION, recorded because it is the reviewable part. The drag PROPOSES; it writes nothing. A stage-writing board endpoint would skip the approval gate, and the tempting alternative — queue a PendingApproval for advance_candidates — is a PRIVILEGE ESCALATION: platform ApprovalExecutor.cs:24-37 re-invokes an approved tool WITHOUT re-checking that tool's permission, and hiring-recruiter deliberately holds ManageApprovals while NOT holding tools.hiring.advance_candidates (Program.cs:58-60). Either route hands the recruiter the one decision the role model withholds. L1 on the code fact (read from platform source), L4 on the consequence — not demonstrated, because demonstrating it is what this avoids.
2026-08-07T18:30Z · deliver · NOTE · rule 1 MATCHED and was OUT OF BOUNDS this run · #38 and #41 both carry agent:changes-requested, so rule 1 outranked rule 4 — but both touch ONLY .github/ files and this run's hard limits forbid editing .github/. Not silently skipped. They are structurally human-gated anyway (spine_untouched fires on every .github/ diff), so no agent can advance them regardless. Fell through to rule 4 with 2 open PRs < maxOpenPRs 3. Open PRs now 3/3, so the next deliver tick will be No-op on the ceiling until someone reviews.
2026-08-07T18:30Z · deliver · NOTE · #35 REPRODUCED · first boot gave 403 for hiring-sourcer on the new endpoint: the grant was correct in code and never reached the existing database. `docker volume rm hireworthy-pg-data` + reboot → 200. That is issue #35 with a reproduction attached.
2026-08-07T18:30Z · deliver · NOTE · two gaps found in passing, NOT fixed (out of scope for #15) · (1) `hiring-manager` is in PLAN.md §7 with advance_candidates but is NOT defined in Program.cs, so "advancing is the hiring manager's call" is enforced by nobody but hiring-talent-lead; (2) frontend/hireworthy-ui declares `"test": "vitest run"` with no vitest dependency and no test files, which makes the frontend rung look covered when it is not. Both flagged in the PR body.

2026-08-07T17:35Z · ship · 3 open · 1 reviewed · 0 merged · Success (reviewed and correctly blocked) · #42 REVIEWED (created 17:17Z by a concurrent deliver tick, one minute into this one — unlabelled, 0 reviews) → agent:changes-requested, 2 blocking: (1) ARCH.md:80 still promises the drag-to-MOVE this PR deliberately refuses, and the reversal is argued only in three code comments and a PR body that goes invisible after merge — the next tick reads that line, sees an unimplemented write, and to build it must first delete `PipelineTests.The_board_offers_no_route_that_moves_a_candidate_between_stages`, which will read as tidying a test that blocks a documented feature; pr-gates.mjs will not catch it because CONTENT_RULES scan removed lines for RequiresApproval|Permissions.|AddPlenipoRole|HasQueryFilter and a new MapPut matches none of them; DECISIONS.md stops at ADR-0011, written for this exact tab; (2) PipelineTests.cs:193-201 `list_applicants_is_registered_and_is_not_approval_gated` ends in a substring match on catalog.GetRawText(), so the `is_not_approval_gated` half is asserted by NOTHING — and that is precisely the property the PR body offers the human holding the permission grant ("the grant is a READ, RequiresApproval: false, verified in the security catalog"), while SmokeTests pins requiresApproval explicitly for six of nine tools and not this one. The body's central design argument SURVIVED the attack: ApprovalExecutor.cs:24-37 has no permission check, ToolRegistry.cs:16-35 does not filter either (the way the argument could have been wrong), ApprovalEndpoints.cs:83 gates approve on ManageApprovals alone, and AuthorizedAgentRunner.cs:274-284 is the only filter — so a drop that queued a PendingApproval really would hand hiring-recruiter the advance it is deliberately denied. Permission widening correctly scoped: SPEC.md:123-130 lists tools.hiring.list_applicants in the sourcer baseline verbatim, Program.cs:39/:58 grant that and nothing else. Verdict is L4 throughout — the reviewer ran nothing; build+PR gates green are L1 but are CI on the author's own code · #38 and #41 unchanged since 16:51Z and NOT re-reviewed: both verdicts still post-date their newest commit (#38 16:15:18Z over 16:10:06Z, #41 16:30:54Z over 13:15:05Z), so neither is stale under the very rule #38 exists to add · `--merge` not run: 0 READY, and no gate blocking any of the three can turn green without a human · preflight L1: stage=product, gh auth green, protection on main with required contexts `PR gates` + `build`, main green, autonomy level 3 READ from workflow.json
2026-08-07T17:35Z · ship · NOTE · the domain hold on #42 was checked at L4 rather than inherited from the author's word. The diff was grepped for RequiresApproval, HasQueryFilter, AddPlenipoRole, Permissions., consent and impact: the only non-comment hits are four `Permissions.ForTool(…, "list_applicants")` sites and the two AddPlenipoRole additions the body already declares. No approval flag, no tenant query filter, no consent path, no impact-report path — so no Approval-required hold beyond the `needs-human` its author already applied, and this tick did not and will not apply `human-approved`. Worth recording why `PR gates` is GREEN on a PR that widens a permission grant: pr-gates.mjs:111 scans only `-` lines, so a pure widening inside AddPlenipoRole is structurally invisible to spine_untouched. Green there is not evidence the grant was reviewed — the same gap that escalated #36, now confirmed a third time.
2026-08-07T17:35Z · ship · NOTE · the reviewer surfaced a LIVE privilege escalation in the shipped product, pre-existing and unfiled. hiring-recruiter holds Permissions.ManageApprovals (Program.cs:63) and NOT tools.hiring.advance_candidates, but ApprovalEndpoints.cs:83 gates approve on ManageApprovals alone and ApprovalExecutor re-invokes without re-checking the tool's own permission — so a recruiter can approve an advance_candidates request queued by a hiring-talent-lead and move a candidate the role model withholds from them. On a product that decides who gets a job that is not a nit. Not filed by this tick (ship does not open issues, and filing mid-flight is the anti-pattern) but it needs a Hireworthy issue AND a platform request via /deliver:request-platform-change, because the fix is in the platform's ApprovalExecutor, not in this product. Non-blocking for #42, which correctly refuses to widen the exposure.
2026-08-07T17:35Z · ship · NOTE · still not Blocked on the standing 401. `agent` is IN_PROGRESS on #42 and FAILURE on #41, SUCCESS on #38 — real, and a human still owns that token — but the Blocked test is that the 401 be the ONLY thing between a PR and merge, and it is not for any of the three: #38 and #41 each need a human `human-approved` for spine_untouched plus a revision plus a rebase off BEHIND, and #42 needs a human on the permission grant plus a revision. Fixing the token today merges nothing. Not Approval-required either: that state wants a PR that cleared every automated gate, and none did.

2026-08-07T18:01Z · ship · 3 open · 0 reviewed · 0 merged · No-op · 26 minutes after the 17:35Z tick and nothing had changed hands. Step 3 had no candidate: every open PR carries an adversarial verdict that post-dates its own newest commit — #38 16:15:18Z over 16:10:06Z, #41 16:30:54Z over 13:15:05Z, #42 17:28:01Z over 17:15:58Z — so none is stale under the rule #38 exists to add, and all three are `agent:changes-requested`. `merge-gate.mjs` dry-run: 0 ready · 3 blocked · 0 merged. #38 checks_green(PR gates) + mergeable(BEHIND) + agent_approved; #41 checks_green(PR gates, agent) + mergeable(BEHIND) + agent_approved; #42 mergeable(BEHIND) + agent_approved + no_human_hold(needs-human). `--merge` not run: no candidate, and no gate blocking any of the three can turn green without a human. Preflight L1: stage=product, gh auth green, protection on main with required contexts `PR gates` + `build`, main green, autonomy level 3 READ from workflow.json. Not Approval-required: that state wants a PR that cleared every automated gate, and none did.
2026-08-07T18:01Z · ship · NOTE · the standing 401 blocker is RESOLVED, and this is the correction the next tick needs. The `Give one pull request an approval verdict` workflow now SUCCEEDS: run 31196261350 at 16:10:14Z on #38 and run 31201577051 at 17:17:06Z on #42 both conclusion=success, and `agent` reads pass on both PRs. The only FAILURE left in the repo is run 31181776488 at 13:15:14Z on #41 — it PREDATES both successes and is simply the last run that branch has had, since #41's head has not moved since 13:15:05Z. So #41's red `agent` is a stale artifact of the outage, not a live 401; the rebase it needs anyway off BEHIND will re-fire it. The 17:35Z tick saw this half-formed (it recorded `agent` IN_PROGRESS on #42) and correctly declined to call it. Nothing about this changes any terminal state: the 401 was never the binding constraint on any of the three, and its repair merges nothing.
2026-08-07T18:01Z · ship · NOTE · re-confirming the gap under #42 because it is the one thing here that could quietly go wrong, and it is now load-bearing. #42 widens permission grants — `tools.hiring.list_applicants` added to the hiring-sourcer and hiring-recruiter baselines at Program.cs:39/:58 — and its `PR gates` check is GREEN. Read at L1, not inherited: pr-gates.mjs:80-85 CONTENT_RULES are matched only inside `if (line.startsWith('-'))` at :111, whose own comment says it leaves "pure additions alone". A grant is by definition an addition, so spine_untouched cannot see one. workflow.json //what-3-means-here names those four CONTENT_RULES as what enforces the //domain-note, and for the permission-grant class it does not. The ONLY thing holding #42 is the `needs-human` label its own author volunteered — remove that label and this PR merges on gates alone. Diff re-grepped this tick: no consent path, no impact-report path, no RequiresApproval or HasQueryFilter edit, so no Approval-required hold beyond the one already set. `human-approved` not applied, and it is not mine to apply.

2026-08-07T18:05Z · test · swept 52ac19f · Success · 7 filed (2 p0, 2 p1, 3 p2) · 0 deduped, 0 dropped under the cap of 8 · L3 · FIRST sweep of this product — no prior `test` line existed, so there was no watermark and nothing to skip against. Swept `origin/main` in a detached worktree under the scratchpad rather than in this checkout, which was mid-flight on feat/15-pipeline-board when the sweep started (the concurrent ship tick has since moved it to main). Journeys walked: cold start + empty tenant, the core loop (requisition → rubric → CV → screening → advance/reject), the approval gate, RBAC with three narrowed roles, tenant isolation across two tenants, ranking integrity, both read surfaces + the UI, consent/impact probe, and the runtime-observable invariants. Ladder green on the swept sha: build 0 warnings, 37/37 unit, 55/55 integration incl. 9 golden evals.
2026-08-07T18:05Z · test · NOTE · the headline is #44 and #45, both p0, and BOTH are score arithmetic that decides who gets shortlisted · `ScreeningResult.ComputeTotal` (ScreeningResult.cs:87-91) builds the maximum by iterating the SUPPLIED scores, not the rubric's criteria, and `screen_applicant` validates neither completeness nor distinctness. Observed: APP-1002 scored 37/90 (41.1%) with all five criteria, and 25/25 (100%) supplying only the one criterion its CV evidences — outranking APP-1001, who honestly scores 88.9%. The same criterion passed twice is counted twice, which does not merely inflate a total but re-weights the rubric for one applicant. Priced p0 under the domain rule rather than the skill's table, which would call a wrong number on a read surface p1: this product decides who gets a job, so a defect that silently changes rank is p0 even when it looks cosmetic. ADR-0004 keeps an `unresolved` criterion inside the maximum precisely so a vague CV cannot shrink the yardstick — omitting the criterion achieves that outcome more cheaply, and nothing checks.
2026-08-07T18:05Z · test · NOTE · a fully green ladder is exactly what made this worth sweeping · `ScreeningTests` proves citations are grounded and an unapproved rubric is refused; NOTHING asserts the score set is complete or distinct with respect to the rubric. That is the hole, and it is the thing to see red first. Also unwatched and now filed: #46 approving a parked write writes no audit row and every `hiring.decisions` row has a null `ApprovedBy` — the audit database's own stated purpose in AppHost.cs is "who advanced or rejected which candidate, on what evidence", and it does not answer "who"; #47 `hiring-compliance` is granted only chat.use + chat.conversations.view, so it 403s on the audit trail its role comment says it exists to read (NOT a duplicate of #35, but blocked behind it — the grant will not reach a seeded volume, and the diff widens a permission grant so pr-gates will hold it for a human).
2026-08-07T18:05Z · test · NOTE · consent and adverse-impact are ABSENT, not broken, and that is reported as a coverage gap rather than filed as a bug · no consent tool, route, entity or column; no selection-rate calculation anywhere. Nothing computes a wrong rate because nothing computes a rate. The runtime fact worth carrying: on this sha `screen_applicant` — an AI evaluation of a named person — runs with no consent record AND no approval gate, and a `hiring-sourcer` can drive it (verified: the sourcer's turn executes, no approval_required event). SPEC J6 calls that gate blocking. Recorded as a comment on #16 with L3 evidence, no labels or board state touched. Every gate that DOES exist fired correctly — propose_rubric, advance_candidates and reject_candidate all park, execute nothing and never claim success — so this is a gate that does not exist yet, not one that regressed.
2026-08-07T18:05Z · test · NOTE · what the sweep could NOT reach, because a gap nobody hears about is inherited · (1) #44/#45 were driven by calling the tool directly, since the Mock provider cannot emit a populated `scores[]` — the arithmetic, writes and read surfaces are shipped code and nothing between a real provider and the database checks, but no model drove it; (2) no SUCCESSFUL approval-gated execution end to end, same cause (Mock fills every array arg with []), so #46 rests on an ERRORED approved execution going unaudited — a successful one is not ruled out; (3) no Aspire MCP in session, so telemetry was read from stdout and CLI logs, not traces. Environment, not product: the AppHost was killed three times mid-sweep (DCP SocketException 10054) while other Plenipo stacks cycled on this machine; nothing above depends on a run that died. Two cosmetics deliberately NOT filed: `aspire run` drops an untracked, ungitignored `aspire.config.json` in the repo root, and the pipeline board's absence leaves no in-app path to a candidate (#48 covers the nav link that dead-ends).
2026-08-07T18:05Z · test · NOTE · next sweep's skip check · main is now 299b4ef, but 52ac19f→299b4ef is journal lines only, no code delta. If nothing else merges, the honest next state is No-op even though the sha moved — compare trees, not shas.

2026-08-07T18:17Z · deliver · rule 1 · PR #42 · Success · revised the rejected PR: both blocking findings fixed, pushed a9c18a0→1e00add, agent:changes-requested removed so ship re-evaluates · L1 build 0-warn + 37 module + 62 integration on the revised head; the new assertion seen RED first by temporarily setting RequiresApproval = true on the list_applicants ToolDescriptor, then reverted (git diff src/ empty) · card stays In Review, needs-human stays, human-approved NOT applied
2026-08-07T18:17Z · deliver · NOTE · rule 1 · — · WHY #42 AND NOT #38/#41 · all three open PRs carried agent:changes-requested, so rule 1 fired three times over. #38 and #41 touch ONLY .github/ files and this run's hard limits forbid editing .github/, so they were out of bounds — not skipped for convenience, and the 18:30Z tick had already recorded the same. They are structurally human-gated anyway (spine_untouched fires on every .github/ diff), so no agent can advance them regardless. #42 is a product PR and was in bounds. Rule 4 was closed independently: 3 open PRs = maxOpenPRs 3, so review is the constraint and no new issue could have been started this tick even had rule 1 been empty.
2026-08-07T18:17Z · deliver · NOTE · PR #42 · — · WHAT THE TWO FIXES WERE, because the second is the one that matters · (1) ARCH.md:80 still promised "Drag-and-drop across stage columns" while the delivered board refuses the write, and the reversal was argued only in code comments and a PR body that goes invisible after merge — now ADR-0012, which names the guard test and states that pr-gates.mjs CANNOT catch its deletion (CONTENT_RULES scan removed lines for RequiresApproval|Permissions.|AddPlenipoRole|HasQueryFilter; a new MapPut matches none). (2) PipelineTests.list_applicants_is_registered_and_is_not_approval_gated ended in Assert.Contains on catalog.GetRawText(), so the is_not_approval_gated half was asserted by NOTHING — and that is exactly the property the PR body offers the human holding the permission grant. Now parsed the way SmokeTests.cs:92-97 does. No product code changed in this pass: git diff a9c18a0..1e00add -- src/ is empty, so the original L3 board evidence still describes the code and was deliberately not re-run rather than restated as if it had been.
2026-08-07T18:17Z · deliver · NOTE · PR #42 · — · PLATFORM CITATIONS RE-READ, NOT INHERITED · the reviewer's escalation argument was checked against the plenipo checkout rather than taken on its word, because ADR-0012 now carries it durably: AuthorizedAgentRunner.cs:277-284 is the only per-tool gate (filters candidateTools on currentUser.HasPermission), ApprovalExecutor.cs:24-37 resolves by module+name and calls tool.Function.InvokeAsync with NO permission check, ApprovalEndpoints.cs:82 gates approve on ManageApprovals alone. Checkout is v0.1.0-alpha.28-30-g0b238d4 and ApprovalExecutor.cs last changed at 4a203c7 (2026-07-13), before the tag — so the read matches the vendored 0.1.0-alpha.28 nupkg. One correction to the review: it cited the approve gate at :83; it is :82 here. Same statement.
2026-08-07T18:17Z · deliver · NOTE · #51 FILED, p0, and it is a LIVE hole in shipped code · a hiring-recruiter holds Permissions.ManageApprovals (Program.cs:66) and deliberately not tools.hiring.advance_candidates (Program.cs:64-65, SPEC.md §3 "advancing is the hiring manager's call"), but the platform enforces a tool's permission when a request is CREATED and never when it is EXECUTED — so a recruiter can approve an advance queued by a talent-lead and make the decision their tier is denied. Pre-existing on main; #42 neither causes nor fixes it and ADR-0012 records that it declines to build on top of it. NOT MITIGATED. The fix is in the platform's ApprovalExecutor and AGENTS.md forbids editing the platform from here, so the named next action is /deliver:request-platform-change — NOT done this tick and not silently dropped: it is a distinct skill with its own escalation ladder, and filing it mid-revision is the stop-delivering-to-improve-the-harness anti-pattern. L4 throughout: read from source on both sides, never executed. Demonstrating it means actually advancing a candidate through the hole.
2026-08-07T18:17Z · deliver · NOTE · #52 FILED, p2, and the one-line fix was REFUSED on purpose · GET /api/hiring/pipeline (HiringModule.cs:634-654) has no row bound while the list_applicants tool caps at Take(200). Adding .Take(200) to match would have closed the reviewer's finding in one line and made the product worse: a hiring board that silently drops applicants past a cap makes a real person invisible to the recruiter working the pile, with nothing on screen saying so. On a product that decides who gets a job that is a worse failure than slow. Filed with three options (bound + visible remainder, paginate, or a documented ceiling) rather than absorbed into a review round. L4 and explicitly NOT observed at runtime — no requisition in the seed data or any test is near a size where this is measurable, so there is no reproduction and no evidence of a real problem today.
2026-08-07T18:17Z · deliver · NOTE · two non-blocking findings answered with NO CHANGE, recorded so silence does not read as oversight · (1) the SmokeTests.cs:84 comment about the security catalog is factually wrong (the catalog is built from catalog.Manifests only, AdminEndpoints.cs:721-750; ManifestGuardTests.cs:73-84 is what actually enforces tool/manifest parity) — it predates this PR, and a drive-by comment edit does not belong in a diff a human is reviewing for a permission grant; (2) hiring-manager missing from Program.cs and pnpm test declared with no vitest were already filed by the overnight sweep as #49 and #50.
2026-08-07T18:17Z · deliver · NOTE · clock · GitHub stamps this tick's first workflow run at 18:16:31Z, so 18:17Z above is real time cross-checked against a second source. The preceding deliver entry is stamped 18:30Z, which is ahead of it — that entry's timestamp is wrong, not this one. Flagged because an out-of-order append-only journal otherwise reads as a lost tick.

2026-08-07T18:21Z · ship · 3 open · 1 reviewed · 0 merged · Success (reviewed, then correctly blocked) · #42 RE-REVIEWED and approved — the first `agent:approved` this repo has issued. It was reviewed despite carrying a verdict because that verdict was STALE under the very rule #38 exists to add: `agent:changes-requested` was posted 17:27:59Z and two commits landed after it (6a87b3a ADR-0012 + ARCH.md:80, 1e00add the requiresApproval assertion), both answering the two blocking findings exactly and touching nothing else. Delegated to `pr-reviewer` from a context that wrote none of it. Both fixes verified as real, not claimed: ARCH.md:80 now says the drop writes nothing and cites ADR-0012, and DECISIONS.md:282 is a real ADR that names both rejected alternatives AND warns that a future stage-writing route must first delete the guard test, "which will read as tidying a test that blocks a documented feature"; PipelineTests.cs:209-220 replaces the substring match with a parsed `Assert.False(tools[…].GetProperty("requiresApproval").GetBoolean())` whose parse path is copied from SmokeTests, which already asserts that same field True for three tools and False for four — so the field carries a real per-tool boolean and the assertion can actually go red, which the substring could not. No check bent: SmokeTests went `Assert.Equal(8→9)` AND gained an explicit ContainsKey, so the inventory is still pinned; the new eval case's `forbidToolCalls` is really iterated by GoldenEvalTests.Case, not an ignored field; no test deleted, no query filter removed, no RequiresApproval flipped · L4 throughout — the reviewer ran nothing. All ten checks green on 1e00add including `agent`, which is L1 but is the author's CI over the author's own code · #38 and #41 NOT re-reviewed and unchanged: their verdicts still post-date their heads (16:15:17Z over 16:10:06Z; 16:30:52Z over 13:15:05Z), so neither is stale, and no gate blocking either can turn green without a human · `--merge` RUN: 0 ready · 3 blocked · 0 merged · preflight L1: stage=product, gh auth green, classic protection on main with required contexts `PR gates` + `build`, autonomy level 3 READ from workflow.json (not inferred)
2026-08-07T18:21Z · ship · NOTE · #42 IS HELD FOR A HUMAN AND THIS TICK DID NOT SELF-CLEAR IT · after the approval its remaining gates are `no_human_hold: needs-human` · `mergeable: BEHIND` · `checks_green: 1 running` (my own label edit re-fired a workflow). Only the first matters: HOLD_LABELS at merge-gate.mjs:58 is `['human-hold','needs-human','agent:blocked']` and :202 fails unconditionally on any of them — `human-approved` does NOT clear `needs-human`, so a human must REMOVE that label, and removing it is the whole decision. What they are being asked to decide is stated in the PR comment: the PR widens permission grants (tools.hiring.list_applicants onto hiring-sourcer and hiring-recruiter, Program.cs:40/:58), which workflow.json //domain-note holds human at EVERY level including this repo's 3. The grant was checked here rather than inherited: SPEC.md:126 lists it verbatim in the sourcer baseline, PLAN.md:194 agrees, the tool is a read, both baselines are enumerated allowlists not wildcards, and both roles already hold get_applicant which returns full CV text — so no enumeration class is newly exposed. Diff re-grepped for consent|adverse|impact|disparate|four-fifths|EEOC: exactly one hit and it is an unchanged CONTEXT line in the ARCH.md tab table, so no consent or impact-report surface is touched and no extra hold applies. `human-approved` not applied and not mine to apply.
2026-08-07T18:21Z · ship · NOTE · terminal state chosen deliberately, and it is NOT Approval-required · that state wants a PR that cleared EVERY automated gate and is held only by level or diff class, and #42 has not: it is BEHIND and one check was still running. Success is the literal fit — "at least one reviewed and correctly blocked". Worth recording for the next tick that under this script a `needs-human` PR can never REACH Approval-required by itself, because no_human_hold is evaluated alongside the mechanical gates rather than after them; the state will keep reading Success or No-op no matter how long the human sits on it. Also NOT Blocked: the standing Copilot-provider 401 is fully recovered here — all ten checks on 1e00add are SUCCESS including `agent` — and it was never the binding constraint on #42 anyway.
2026-08-07T18:21Z · ship · NOTE · what this tick did not do, so silence is not read as coverage · did not rebase #42 off BEHIND (ship never touches a branch, and a push would re-stale the verdict it just issued), did not re-comment the gate reasons on #38/#41 (both already carry a current one from earlier ticks; a second is the hundred-identical-comments pitfall), and did not file the platform request for #51 that the 18:17Z deliver NOTE names as the next action — filing mid-tick is the stop-delivering-to-improve-the-harness anti-pattern and /deliver:request-platform-change is its own skill with its own ladder. #51 remains the highest-value open thread in this repo: a live privilege escalation in shipped code, on a product that decides who gets a job.

2026-08-07T18:50Z · ship · 3 open · 0 reviewed · 0 merged · **Approval-required** · #42 has now cleared every automated gate it can clear and is held only by a human decision it is not this loop's to make. `merge-gate.mjs` dry-run and `--merge` both: 0 ready · 3 blocked · 0 merged. #42's remaining gates are `no_human_hold: needs-human` and `mergeable: BEHIND`; all eleven checks on `1e00add` are SUCCESS including `PR gates`, `build` and `agent`, so the "1 running" the 18:21Z tick recorded has landed green and `checks_green` now passes. Step 3 had no candidate: every open PR carries a verdict that post-dates its own head — #38 16:15:17Z over 16:10:06Z, #41 16:30:52Z over 13:15:05Z, #42 18:21:14Z over 18:12:41Z — so nothing is stale under the rule #38 exists to add, and nothing was re-reviewed. #38 and #41 were not re-commented (both carry a current gate comment; a second is the hundred-identical-comments pitfall). One gate comment posted on #42 carrying a `<!-- plenipo-merge-gate pr=42 -->` marker so the next tick edits it instead of appending. Preflight L1: stage=product, gh auth green, classic protection on main with required contexts `PR gates` + `build`, main green, autonomy level 3 READ from workflow.json, never inferred.
2026-08-07T18:50Z · ship · NOTE · the terminal state is a REVERSAL of the 18:21Z tick's reading, and the reversal is the point · that tick recorded that "under this script a `needs-human` PR can never REACH Approval-required, because no_human_hold is evaluated alongside the mechanical gates rather than after them." That over-generalised from its own moment: #42 was then BEHIND *and* had a check still running, so it genuinely had not cleared the automated gates. It has now. The only mechanical gate left is `mergeable: BEHIND`, and clearing it first would be actively wrong — updating the branch moves the head and stales the 18:21Z `agent:approved` under the rule #38 adds, forcing a fresh adversarial review before a merge that is waiting on a human anyway. So BEHIND is downstream of the decision, not a competing blocker, and Approval-required is the honest fit: an agent has gone as far as an agent can. Recorded plainly that `mergeable` is still red so the claim is not overstated as "every gate green".
2026-08-07T18:50Z · ship · NOTE · the domain hold on #42 was re-checked at L1 this tick rather than inherited from two earlier ticks that said the same thing · `gh pr diff 42` grepped for `^[+-].*(RequiresApproval|HasQueryFilter|AddPlenipoRole|Permissions\.)`: eight hits, ALL additions, no removal or edit of any of the four classes. Six are ADR prose or tool/endpoint declarations; the two that matter are `Program.cs` adding `"tools.hiring.list_applicants"` to the hiring-sourcer and hiring-recruiter baselines — pure permission grants, which `workflow.json` `//domain-note` holds human at EVERY level including 3, and which `pr-gates.mjs` structurally cannot see because its CONTENT_RULES only scan `-` lines (:111). Grep for `consent|adverse|impact|disparate|four-fifths|EEOC|selection.rate`: exactly one hit, an unchanged CONTEXT line in the ARCH.md tab table, so the scheduled task's extra hold on consent and impact-report paths does not fire here. `human-approved` NOT applied and not mine to apply; `needs-human` NOT removed; `autonomy.level` NOT touched; nothing under `.github/` read-modified.
2026-08-07T18:50Z · ship · NOTE · NOT Blocked, and the standing 401 note in the scheduled task is now stale in the task itself · the task carries "the `Give one pull request an approval verdict/agent` check fails with HTTP 401 on every PR here." It does not: `agent` is SUCCESS on #38 (run 31196261350) and SUCCESS on #42 (run 31206002464). The single remaining FAILURE is run 31181776488 at 13:15:14Z on #41, which predates both successes and is simply the last run that branch has had, since #41's head has not moved since 13:15:05Z — a stale artifact of the outage, not a live 401, and the rebase #41 needs off BEHIND will re-fire it. The 18:01Z tick called this first; this tick confirms it a second time. Worth a human updating the task text, because a standing blocker that has healed will otherwise keep being cited as a reason not to look.
2026-08-07T18:50Z · ship · NOTE · #38 and #41 remain structurally out of reach of this loop and that has not changed · both fail `checks_green` on `PR gates`, both are BEHIND, both carry a current `agent:changes-requested`, and both touch ONLY `.github/` — which this run's hard limits forbid editing and which `spine_untouched` holds for a human regardless. No agent tick, ship or deliver, can advance either. Also unchanged and still the highest-value open thread in this repo: #51, the live privilege escalation where a hiring-recruiter holding ManageApprovals can approve an `advance_candidates` the role model denies them. Its named next action is `/deliver:request-platform-change`, which is not ship's and was not done here.
